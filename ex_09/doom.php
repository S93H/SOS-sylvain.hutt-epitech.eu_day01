<?php

function draw_triangle($size) {
    
    for ($row = 1; $row <= $size; $row++) {
        for ($space = 1; $space <= $size - $row; $space++) {
            echo "  ";
        }
    }

    while ()
    
    

}